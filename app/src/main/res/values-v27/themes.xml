<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- windowLayoutInDisplayCutoutMode требует API 27+ (minSdk у нас 26) —
         переопределение темы только для устройств, которые это реально поддерживают. -->
    <style name="Theme.TennisAnalyzer" parent="android:Theme.Black.NoTitleBar.Fullscreen">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
